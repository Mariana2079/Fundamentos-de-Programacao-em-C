/*4) Construa um algoritmo que, dado um conjunto de valores inteiros e positivos, determine qual o menor e o maior valor positivos informados.
Dos valores negativos calcular a média. O final do conjunto de valores é conhecido através do valor -1, que não deve ser considerado.*/

#include<stdio.h>
int main(void)

{
    int num,maior,menor,qtde=0,soma=0;
    char primeiro='n';

    do
    {
        printf("Informe um numero: ");
        scanf("%d",&num);
    }
    if(primeiro='n')
    {
        maior=num;
        menor=num;
        primeiro='s';
    }
    if(num>)
}

